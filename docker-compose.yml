services:
  jarvis:
    build: .
    environment:
      JARVIS_CONFIG_PATH: /app/config.docker.yaml
    depends_on:
      - ollama

  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama:/root/.ollama
    ports:
      - "11434:11434"

volumes:
  ollama:
